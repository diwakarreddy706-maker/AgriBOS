services:
  # 1. AgriBOS Backend Web Service
  - type: web
    name: agribos-backend
    runtime: node
    plan: free
    buildCommand: pnpm install --frozen-lockfile
    startCommand: pnpm --filter @agribos/backend start
    healthCheckPath: /api/v1/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: REFRESH_SECRET
        generateValue: true
      - key: FRONTEND_URL
        fromService:
          type: web
          name: agribos-frontend
          envVarKey: RENDER_EXTERNAL_URL

  # 2. AgriBOS Frontend Static Site
  - type: web
    name: agribos-frontend
    runtime: static
    buildCommand: pnpm install --frozen-lockfile && pnpm --filter @agribos/frontend build
    staticPublishPath: apps/frontend/dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_BASE_URL
        fromService:
          type: web
          name: agribos-backend
          envVarKey: RENDER_EXTERNAL_URL
